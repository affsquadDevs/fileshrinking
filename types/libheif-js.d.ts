// libheif-js ships no high-level type declarations for its bundled build.
// Ambient declaration (this file must stay free of import/export so it is
// treated as a global script, not a module).
declare module "libheif-js/wasm-bundle";
